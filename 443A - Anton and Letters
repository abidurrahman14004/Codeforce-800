#include <bits/stdc++.h>
using namespace std;

int main(){
    char  s[1000];
    cin.getline(s,1000);
    set<char> v;
    for(int i=0;i<strlen(s);i++){
        if(s[i]==' ' || s[i]==',' || s[i]=='{' || s[i]=='}') continue;
        v.insert(s[i]);
    }
    cout<<v.size()<<endl;

    return 0;
}
