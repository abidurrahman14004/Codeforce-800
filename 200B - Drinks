#include<bits/stdc++.h>

using namespace std;

int main(){
    int n;
    double sum=0.0,ans=0.0; cin>>n;
    int arr[n];
    for(int i=0; i<n; i++){
        cin>>arr[i];
        sum+=arr[i];
    }

    ans=double(sum/n);
    
   
    cout<<fixed<< setprecision(12)<<ans<<endl;


    return 0;
}
