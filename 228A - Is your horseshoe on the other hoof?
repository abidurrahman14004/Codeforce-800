#include<bits/stdc++.h>
#include<vector>
using namespace std;
int main(){
    int n,count=0;
vector<int>v;
for(int i=0; i<4; i++) {
  cin>>n;
  v.push_back(n);
}
sort (v.begin(),v.end());
for(int i=0;i<3;i++){
    if(v[i]==v[i+1]) count++;
   
} 
cout<<count<<endl;
    return 0;
}
