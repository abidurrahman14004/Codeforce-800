#include<iostream>
using namespace std;

int main(){
    int n,h,x;
    cin>>n>>h;
    int sum=0;
    while(n--){
        cin>>x;
        if(x>h) sum=sum+2;
        else sum++;
    }
  cout<<sum<<endl;
    return 0;
}
