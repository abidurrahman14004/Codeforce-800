#include<iostream>
#include<vector>
using namespace std;

int main(){
int n,x;
vector<int>v(4);
cin>>n;
while(n--){
for(int i=0;i<4;i++){
    cin>>v[i];
   }
x=v[0];
int count=0;
for(int i=1;i<4;i++){
    if(x<v[i]) count++;
}
cout<<count<<endl;
}

    return 0;
}
