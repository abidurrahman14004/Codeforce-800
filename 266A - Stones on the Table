#include<bits/stdc++.h>
using namespace std;

int main(){
    int n; char str[1000];
    cin>>n>>str;
    int count=0;
    for(int i=0; i<strlen(str)-1; i++){
        if(str[i]==str[i+1]) count++;
    }
cout<<count<<endl;
return 0;
}
